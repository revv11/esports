import React from "react";
import img from '../images/Frame_193.png'

function Header() {
  return (
    <header style={{backgroundImage: `url(${img})`}}>
    </header>
  );
}

export default Header;
